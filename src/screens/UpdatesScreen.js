import React, { Component } from 'react'
import { View, Text, StyleSheet } from 'react-native'

// Android: List of Google Material Design-looking Card objects, locally generated auto-update text or messages from server?
// iOS: native-looking control with same function
class UpdatesScreen extends Component{
    render(){
        
        return(
            <View>
            </View>
        )
    }
}

const styles = StyleSheet.create({

});

export default UpdatesScreen