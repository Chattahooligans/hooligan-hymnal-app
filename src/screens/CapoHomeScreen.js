import React, { Component } from 'react'
import { View, Text, StyleSheet } from 'react-native'

// List of all available songs, select one and [send notification] button
// Need to think a bit more about how exactly this will look/work
// Other future features for on-device composition and capo to capo communication
class CapoHomeScreen extends Component{
    render(){
        
        return(
            <View>
            </View>
        )
    }
}

const styles = StyleSheet.create({

});

export default CapoHomeScreen